interface:
  display_name: "oneAPI"
  short_description: "Build and debug oneAPI with bundled upstream docs"
  default_prompt: "Use $oneapi to inspect my toolchain and solve this SYCL or oneAPI task using the bundled references."
